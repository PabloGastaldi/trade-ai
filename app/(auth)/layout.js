export const metadata = {
  title: 'trade.ai — Iniciar sesión',
}

export default function AuthLayout({ children }) {
  return (
    <div className="bg-surface min-h-screen">
      {children}
    </div>
  )
}
